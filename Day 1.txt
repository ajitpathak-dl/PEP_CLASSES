public static int solve(int[] nums) { // add your code here 
   int arr[]=nums.clone();
   Arrays.sort(arr);
   int count=0;
   for(int i=0;i<nums.length;i++){
    if (arr[i]!=nums[i]){
        count++;
    }

   }
return count;
}